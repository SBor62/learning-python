# 2. Квадрат

import math

def square(side):
    perimeter = 4 * side
    area = side ** 2
    diagonal = side * math.sqrt(2)
    return perimeter, area, diagonal

# Ввод стороны квадрата
side_length = int(input("Введите длину стороны квадрата: "))

# Вычисление параметров квадрата
result = square(side_length)

# Вывод результатов
print(f"Периметр: {result[0]}, Площадь: {result[1]}, Диагональ: {result[2]}")
